{
  "name": "netpanzer-online-core",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "compile": "esbuild index.ts --bundle --outdir=.",
    "compile-watch": "esbuild index.ts --bundle --outdir=. --watch"
  },
  "dependencies": {

  },
  "devDependencies": {
    "@types/node": "^18.16.3",
    "esbuild": "^0.17.7",
    "nodemon": "^2.0.22",
    "typescript": "^4.9.5"
  }
}
