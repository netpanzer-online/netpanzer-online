{
  "name": "netpanzer-online-server",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "start": "node ./worker",
    "compile": "esbuild public/src/app.ts --bundle --outdir=public/src",
    "compile-watch": "esbuild public/src/app.ts --bundle --outdir=public/src --watch",
  },
  "dependencies": {
  },
  "devDependencies": {
    "esbuild": "^0.17.7",
    "nodemon": "^2.0.22",
    "typescript": "^4.9.5"
  }
}
